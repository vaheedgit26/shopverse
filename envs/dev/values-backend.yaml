namespace: shopverse-dev
# ──────────────────────────────────────
# BACKEND
# ──────────────────────────────────────
image:
  uri: 254405569649.dkr.ecr.us-east-1.amazonaws.com
  name: shopverse-backend
  version: v1
  
containerName: backend
imagePullPolicy: Always
terminationGracePeriodSeconds: 30

labels: 
  app: shopverse
  component: backend
  env: dev

deployment:
  name: backend-deploy
  replicas: 1
    
service:
  name: backend-svc
  type: ClusterIP
  port: 8080
  targetPort: 8080

configmaps:
  local:
    - configmap-backend   # file must be "configmap-backend.yaml"
  global:
    - configmap-global    # file name must be "configmap-global"
      
secrets:
  local:
    - secret-backend      # file must be "secret-backend.yaml"
  global:
    - secret-global       # file must be "secret-global.yaml"

podSecurityContext:
  fsGroup: 2000
  runAsNonRoot: true
  runAsUser: 1000

containerSecurityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 1000

resources:
  requests: 
    cpu: "100m"
    memory: "128Mi" 
  limits:
    cpu: "200m"
    memory: "256Mi"

probes:
  livenessProbe:
    httpGet:
      path: /health
      port: 8080
      initialDelaySeconds: 15
      periodSeconds: 30
        
  readinessProbe:
    httpGet:
    path: /health
    port: 8080
    initialDelaySeconds: 10
    periodSeconds: 10
