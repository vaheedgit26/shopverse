apiVersion: v1
kind: ConfigMap
metadata:
  name: global-config
  labels:
    app: platform
data:
  DB_HOST: "{{ .Values.global.db.host }}"
  DB_PORT: "{{ .Values.global.db.port }}"
  APP_ENV: "{{ .Values.global.app.env }}"
