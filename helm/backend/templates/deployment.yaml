apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.deployment.name }}   # {{ .Release.Name }}
  # namespace: {{ .Values.namespace }}
  {{- with .Values.labels }}
  labels:
  {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.deployment.replicas }}
  selector:
    {{- with .Values.labels }}
    matchLabels:
    {{- toYaml . | nindent 6 }}
    {{- end }}
  template:
    metadata:
      {{- with .Values.labels }}
      labels:
      {{- toYaml . | nindent 8 }}
      {{- end }}
      annotations:
        # LOCAL CONFIG checksum (only if exists)
        {{- range (.Values.configmaps.local | default list) }}
        checksum/{{ . }}: {{ include (print $.Template.BasePath "/" . ".yaml") $ | sha256sum }}
        {{- end }}

        # LOCAL SECRET checksum (only if exists)
        {{- range (.Values.secrets.local | default list) }}
        checksum/{{ . }}: {{ include (print $.Template.BasePath "/" . ".yaml") $ | sha256sum }}
        {{- end }}
     
        {{- $gcm := .Values.configmaps.global | default list }}
        {{- $gsec := .Values.secrets.global | default list }}
        {{- $all := concat $gcm $gsec }}

        # GLOBAL CONFIGMAP and SECRET reloader (only if exists)
        {{- if $all }}
        reloader.stakater.com/match: "{{ join "," $all }}"
        {{- end }}
        
    spec:
      # imagePullSecrets:
      # serviceAccountName: {{ include "pharma-service.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
      {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      containers:
        - name: {{ .Values.containerName | default "backend" }}  # backend
          image: "{{ include "shopverse.backend.image" . }}"
          imagePullPolicy: {{ .Values.imagePullPolicy | default "IfNotPresent" }}   # Always # IfNotPresent 
          ports:
            - containerPort: {{ .Values.service.targetPort }} # 8080
              protocol: TCP
              
          {{- with .Values.containerSecurityContext }}
          securityContext:
          {{- toYaml . | nindent 12 }}
          {{- end }}

          envFrom:
            # =========================
            # CONFIGMAPS
            # =========================
            # LOCAL CONFIGMAPS
            {{- range (.Values.configmaps.local | default list) }}
            - configMapRef:
                name: {{ . }}
            {{- end }}
            
            # GLOBAL CONFIGMAPS
            {{- range (.Values.configmaps.global | default list) }}
            - configMapRef:
                name: {{ . }}
            {{- end }}
            
            # =========================
            # SECRETS
            # =========================
            # LOCAL SECRETS
            {{- range (.Values.secrets.local | default list) }}
            - secretRef:
                name: {{ . }}
            {{- end }}
            
            # GLOBAL SECRETS
            {{- range (.Values.secrets.global | default list) }}
            - secretRef:
                name: {{ . }}
            {{- end }}
                            
          {{- with .Values.resources }}      
          resources:
          {{- toYaml . | nindent 12 }}
          {{- end }}

          {{- with .Values.probes.startupProbe }}
          startupProbe:
          {{- toYaml . | nindent 12 }}
          {{- end }}

          {{- with .Values.probes.readinessProbe }}
          readinessProbe:
          {{- toYaml . | nindent 12 }}
          {{- end }}

          {{- with .Values.probes.livenessProbe }}
          livenessProbe:
          {{- toYaml . | nindent 12 }}
          {{- end }}
